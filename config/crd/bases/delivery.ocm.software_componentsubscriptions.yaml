---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: componentsubscriptions.delivery.ocm.software
spec:
  group: delivery.ocm.software
  names:
    kind: ComponentSubscription
    listKind: ComponentSubscriptionList
    plural: componentsubscriptions
    singular: componentsubscription
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ComponentSubscription is the Schema for the componentsubscriptions
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ComponentSubscriptionSpec defines the desired state of ComponentSubscription
            properties:
              component:
                type: string
              destination:
                description: OCIRepository defines details for a repository, such
                  as access keys and the url.
                properties:
                  secretRef:
                    description: Ref assumes that the namespace is the same as whatever
                      the component has that is looking for this ref.
                    properties:
                      name:
                        type: string
                    required:
                    - name
                    type: object
                  url:
                    type: string
                required:
                - secretRef
                - url
                type: object
              interval:
                type: string
              semver:
                type: string
              source:
                description: OCIRepository defines details for a repository, such
                  as access keys and the url.
                properties:
                  secretRef:
                    description: Ref assumes that the namespace is the same as whatever
                      the component has that is looking for this ref.
                    properties:
                      name:
                        type: string
                    required:
                    - name
                    type: object
                  url:
                    type: string
                required:
                - secretRef
                - url
                type: object
              verify:
                items:
                  description: Signature defines the details of a signature to use
                    for verification.
                  properties:
                    name:
                      description: Name of the signature.
                      type: string
                    publicKey:
                      description: Key which is used for verification.
                      properties:
                        secretRef:
                          description: Ref assumes that the namespace is the same
                            as whatever the component has that is looking for this
                            ref.
                          properties:
                            name:
                              type: string
                          required:
                          - name
                          type: object
                      required:
                      - secretRef
                      type: object
                  required:
                  - name
                  - publicKey
                  type: object
                type: array
            required:
            - component
            - destination
            - interval
            - source
            - verify
            type: object
          status:
            description: ComponentSubscriptionStatus defines the observed state of
              ComponentSubscription
            properties:
              latestVersion:
                type: string
              replicatedVersion:
                type: string
            required:
            - latestVersion
            - replicatedVersion
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
